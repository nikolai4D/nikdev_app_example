/**
 * This is an oversimplification of the state.
 * It holds the ui state(id of the currently viewed project id etc...).
 * Cached data should not be stored here but in the store.
 * @type {{credentials: null}}
 */
export const state = {
    credentials: null
}